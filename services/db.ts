/**
 * @deprecated Ce fichier est conservé pour ne pas casser les références TypeScript.
 * L'implémentation Dexie (IndexedDB) a été remplacée par electronDB.ts (SQLite via IPC Electron).
 * Tous les imports doivent utiliser : import { db } from './electronDB';
 *
 * Ce fichier n'exporte aucune valeur utilisable.
 */
